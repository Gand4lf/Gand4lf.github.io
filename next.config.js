/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    images: {
        unoptimized: true,
    },
    assetPrefix: '/',
    basePath: '/Gand4lf.github.io'
}

module.exports = nextConfig